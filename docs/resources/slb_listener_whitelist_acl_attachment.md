---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "st-alicloud_slb_listener_whitelist_acl_attachment Resource - st-alicloud"
subcategory: ""
description: |-
  Attach ACL(s) to an SLB listener and enable access control with white list type.
---

# st-alicloud_slb_listener_whitelist_acl_attachment (Resource)

Attach ACL(s) to an SLB listener and enable access control with white list type.

## Example Usage

```terraform
resource "st-alicloud_slb_listener_whitelist_acl_attachment" "example" {
  listener_id = "lb-1234567890:tcp:80"
  acl_ids     = ["acl-1234567890", "acl-0987654321"]
}
```

## Argument Reference

The following arguments are supported:

* `listener_id` - (Required, ForceNew) The listener ID in the format `load_balancer_id:protocol:port` (e.g. `lb-xxx:tcp:80`).
* `acl_ids` - (Required) List of ACL IDs to attach to the listener.

## Attributes Reference

The following attributes are exported:

* `id` - Same as `listener_id`.

## Import

SLB Listener Whitelist ACL Attachment can be imported using the `listener_id`, e.g.,

```shell
terraform import st-alicloud_slb_listener_whitelist_acl_attachment.example lb-1234567890:tcp:80
```
